#include <stdio.h>
int main(){
    int contador = 0;
    
             while(contador < 12)
             {
             printf("hello world: %d\n", contador);
             contador += 1;
             }
             printf("acabou!\n");
              return 0;
}
